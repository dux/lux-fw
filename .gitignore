/tmp
/log

# example apps carry a real secret once you run them - keep it out of the
# framework repo. starter/*/config/config.yaml is a template ({{secret}}
# placeholder) and stays tracked.
examples/*/config/config.yaml

.yardoc
*.gem
tmp.rb
Gemfile.lock
package-lock.json
*.tmp.*
.DS_Store
